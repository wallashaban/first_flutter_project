import 'package:flutter/cupertino.dart';

class ShopFavouritesScreen extends StatelessWidget {
  const ShopFavouritesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Favourites'),
    );
  }
}